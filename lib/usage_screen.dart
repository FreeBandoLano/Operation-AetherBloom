import 'package:flutter/material.dart';

class UsageScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Usage Screen')),
      body: Center(child: Text('Usage data will appear here')),
    );
  }
}
